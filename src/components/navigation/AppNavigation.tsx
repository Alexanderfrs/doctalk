import React from "react";
// This component is no longer needed as navigation is handled in AppHeader
// Keeping empty component to avoid breaking imports

const AppNavigation: React.FC = () => {
  return null;
};

export default AppNavigation;
